# Normalize text line endings in the repo to LF.
* text=auto

# Shell scripts must stay LF — CRLF makes the Linux VPS fail with
# "/bin/bash^M: bad interpreter". Covers setup-vps.sh, backup.sh, etc.
*.sh text eol=lf
